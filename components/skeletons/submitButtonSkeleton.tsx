import { Button } from '@/components/ui/button';

export default function SubmitButton() {

  return (
    <Button
      type="submit"
      className="w-full text-black border border-gray-200 rounded-full flex items-center justify-center bg-gray-200 rounded-xl"
    >
    </Button>
  );
}